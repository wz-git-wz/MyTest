<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dch.mapper.Mapperlist">
    <select id="list" resultType="com.dch.entity.Test">
        select * from test
    </select>

    <select id="insert" >
        insert into test values(null,#{name},#{sex},#{age})
    </select>

</mapper>